#ifndef VSTAT_HPP
#define VSTAT_HPP

#include "correlation.hpp"
#include "variance.hpp"

#if defined(VSTAT_NAMESPACE)
namespace VSTAT_NAMESPACE {
#endif

#if defined(VSTAT_NAMESPACE)
}
#endif

#endif
